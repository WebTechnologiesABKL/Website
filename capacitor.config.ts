import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.webtechnologiesabkl.wetter',
  appName: 'Wetter Chatbot',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
